# Changelog

## [0.1.0-preview.0] - Unreleased

### Added
- Package skeleton: `package.json`, `.asmref`s into `Quantum.Simulation` and `Quantum.Unity`.
- `PPCState` enum and `PPCCharacter` component; `PPCSystems.AddTo` registration helper; stub `PPCStateSystem`.
- Headless .NET harness (`Tests~`): Quantum CodeGen, simulation compile, session runner and determinism tests.
