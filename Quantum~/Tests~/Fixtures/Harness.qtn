// Harness-only DSL. The harness plays "the game" using the Playground sample's input and bridge
// (compiled alongside, see build.sh); this adds game-level settings the tests need.
// Room for the 16-character cost test (the package doesn't set this; games do).
#pragma max_players 16
